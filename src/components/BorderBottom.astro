---
interface Props {
    inset: number   
    colors: string
}

const {inset, colors} = Astro.props
---
<div 
    class='absolute border-2 border-b-0 rounded-t-full' 
    style={`border-color: ${colors}; inset:${inset}rem; bottom:0`}></div>